version: 1.0
runtime: python3
build:
  commands:
    - pip install --upgrade pip
    - pip install -r requirements.txt
run:
  command: streamlit run app_pdf.py --server.port 8080 --server.address 0.0.0.0
